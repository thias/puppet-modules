#
# See cgitrc(5) or /usr/share/doc/cgit-*/cgitrc.5.html for details
#

# Enable caching of up to 1000 output entries
cache-size=<%= cache_size %>

# Specify some default clone prefixes
#clone-prefix=git://example.com ssh://example.com/pub/git http://example.com/git
<% if clone_prefix -%>clone-prefix=<%= clone_prefix %><% end -%>

# Specify the css url
css=<%= css %>

# Show extra links for each repository on the index page
enable-index-links=<%= enable_index_links %>

# Enable ASCII art commit history graph on the log pages
enable-commit-graph=<%= enable_commit_graph %>

# Show number of affected files per commit on the log pages
enable-log-filecount=<%= enable_log_filecount %>

# Show number of added/removed lines per commit on the log pages
enable-log-linecount=<%= enable_log_linecount %>

# Add a cgit favicon
#favicon=/favicon.ico
<% if favicon -%>favicon=<%= favicon %><% end -%>

# Use a custom logo
logo=<%= logo %>
<% if logo_link -%>logo-link=<%= logo_link %><% end -%>

# Enable statistics per week, month and quarter
<% if max_stats -%>max-stats=<%= max_stats %><% end -%>

# Set the title and heading of the repository index page
#root-title=example.com git repositories
<% if root_title -%>root-title=<%= root_title %><% end -%>

# Set a subheading for the repository index page
#root-desc=tracking the foobar development
<% if root_desc -%>root-desc=<%= root_desc %><% end -%>

# Include some more info about this site on the index page
#root-readme=/var/www/html/about.html
<% if root_readme -%>root-readme=<%= root_readme %><% end -%>

# Allow download of tar.gz, tar.bz2 and zip-files
#snapshots=tar.gz tar.bz2 zip
<% if snapshots -%>snapshots=<%= snapshots %><% end -%>

##
## List of common mimetypes
##

mimetype.gif=image/gif
mimetype.html=text/html
mimetype.jpg=image/jpeg
mimetype.jpeg=image/jpeg
mimetype.pdf=application/pdf
mimetype.png=image/png
mimetype.svg=image/svg+xml
<% mimetypes.each do |mimetype| -%>
<%= mimetype %>
<% end -%>

# Enable syntax highlighting (requires the highlight package)
#source-filter=/usr/libexec/cgit/filters/syntax-highlighting.sh
<% if source_filter -%>source-filter=<%= source_filter %><% end -%>

##
## List of repositories.
## PS: Any repositories listed when section is unset will not be
##     displayed under a section heading
## PPS: This list could be kept in a different file (e.g. '/etc/cgitrepos')
##      and included like this:
##        include=/etc/cgitrepos
##

#repo.url=foo
#repo.path=/var/lib/git/foo.git
#repo.desc=the master foo repository
#repo.owner=fooman@example.com
#repo.readme=info/web/about.html
<% repos.sort_by {|key, value| key}.each do |reponame, repooptions| -%>

repo.name=<%= reponame %>
<% repooptions.sort_by {|key, value| key}.each do |key, value| -%>
repo.<%= key %>=<%= value %>
<% end -%>
<% end -%>

<% if scan_path -%>scan-path=<%= scan_path %><% end -%>
<% if remove_suffix -%>remove-suffix=<%= remove_suffix%><% end -%>
